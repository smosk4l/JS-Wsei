export default class Note {
  constructor(name, content, color, pinned, date) {
    this.name = name;
    this.content = content;
    this.color = color;
    this.pinned = pinned;
    this.date = date;
  }
}
